# Installation

1.  Install the package using `pip`:

    ```
    pip install django-online-issues
    ```

2.  If you want to use the GitLab backend, install the extra dependencies:

    ```
    pip install "django-online-issues[gitlab]"
    ```
